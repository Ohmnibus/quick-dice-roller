<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
	style="@style/Builder.ValueContainer"
	android:layout_width="fill_parent"
	android:layout_height="fill_parent" >
	
	<TextView style="@style/Builder.Label"
		android:id="@+id/lblMessage"
		
		android:layout_alignParentLeft="true"
		android:layout_alignParentTop="true"
		
		android:layout_height="wrap_content"
		android:layout_width="wrap_content"
		
		android:text="@string/app_name"
		/>
	 
	<LinearLayout
		android:layout_width="wrap_content"
		android:layout_height="wrap_content"
		
		android:layout_centerHorizontal="true"
		android:layout_below="@+id/lblMessage"
		
		android:orientation="horizontal"
		android:gravity="center_vertical"
		>
		
		<!-- Modifier Sign -->
		<kankan.wheel.widget.WheelView
			android:id="@+id/wheelSign"
			
			android:layout_height="wrap_content"
			android:minHeight="64dp"
			android:layout_width="64dp"
			/>
		
		<!-- Modifier hundreds -->
		<kankan.wheel.widget.WheelView
			android:id="@+id/wheelHundreds"
			
			android:layout_height="wrap_content"
			android:minHeight="64dp"
			android:layout_width="64dp"
			/>
		
		<!-- Modifier tens -->
		<kankan.wheel.widget.WheelView
			android:id="@+id/wheelTens"
			
			android:layout_height="wrap_content"
			android:minHeight="64dp"
			android:layout_width="64dp"
			/>
		
		<!-- Modifier units -->
		<kankan.wheel.widget.WheelView
			android:id="@+id/wheelUnits"
			
			android:layout_height="wrap_content"
			android:minHeight="64dp"
			android:layout_width="64dp"

			/>

		<TextView style="@style/Builder.Label.Big"
			android:id="@+id/lblType"
			android:layout_width="wrap_content"
			android:layout_height="wrap_content"

			android:text="%"
			/>
	</LinearLayout>
	
</RelativeLayout>